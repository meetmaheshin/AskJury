{
  "name": "askjury-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}